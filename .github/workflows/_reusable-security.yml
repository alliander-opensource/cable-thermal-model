# SPDX-FileCopyrightText: Contributors to the Cable Thermal Model project
#
# SPDX-License-Identifier: MPL-2.0

name: Security (reusable)

on:
  workflow_call:
    inputs:
      severity:
        description: Severities that should fail the build.
        required: false
        type: string
        default: "CRITICAL,HIGH"
      scanners:
        description: Comma-separated Trivy scanners to enable.
        required: false
        type: string
        default: "vuln,misconfig,secret"
      ignore-unfixed:
        description: Ignore vulnerabilities that have no fix available.
        required: false
        type: boolean
        default: true
      upload-sarif:
        description: Upload the SARIF report to GitHub code scanning.
        required: false
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  fs-scan:
    name: Trivy filesystem
    runs-on: mgd-ubuntu24-x64-4c16g
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Trivy filesystem scan
        uses: ./.github/actions/trivy-fs-scan
        with:
          severity: ${{ inputs.severity }}
          scanners: ${{ inputs.scanners }}
          ignore-unfixed: ${{ inputs.ignore-unfixed }}
          upload-sarif: ${{ inputs.upload-sarif }}
